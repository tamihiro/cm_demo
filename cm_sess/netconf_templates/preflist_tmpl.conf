{# template in the xml format.
#}
policy-options {
{% for acl_name, acl in acl_dict.iteritems() %}
    prefix-list {{ acl_name }} {
{% for n in acl %}
        {{ n }};
{% endfor %}
    }
{% endfor %}
}

